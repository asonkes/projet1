{% extends 'base.html.twig' %}

{% block title %}Réinitialisation du Mot de Passe
{% endblock %}

{% block body %}

	<div class="session_container">
		<div class="session">
			<h1>Réinitialisation du Mot de Passe</h1>

			<div method="post" class="form flex align-items">

				{{form_start(passForm)}}

				<div class="flex mt-3">
					{{form_label(passForm.password)}}
					{{form_widget(passForm.password)}}
					{{form_errors(passForm.password)}}
				</div>

				<button type="submit" class="btn btn-md btn-primary mt-3">Envoyer</button>

				{{form_end(passForm)}}

			</div>
		</div>
	</div>
{% endblock %}
